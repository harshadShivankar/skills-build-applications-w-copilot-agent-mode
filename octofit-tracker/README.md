# OctoFit Tracker (scaffold)

This folder contains the OctoFit Tracker project scaffold.

- `backend/` — backend Python/Django project (requirements and virtualenv).
- `frontend/` — frontend application (React or similar).

Next steps:

- Create the Python virtual environment in `backend/venv` and install `requirements.txt`.
- Scaffold a Django project inside `backend/octofit_tracker`.
